61f5290f2 100644
--- a/static/interface_text.js
+++ b/static/interface_text.js
@@ -223,6 +223,7 @@ function buildMySpotsTextEnglish({ appName = DEFAULT_APP_NAME, nimiqPayUrl = DEF
             confirm: 'Confirm',
             confirming: 'Cancelling…',
             cancel: 'Cancel',
+            manualReviewNotice: 'Failed deposit records will remain attached to this Spot for manual review and are not included in the estimated refund.',
             confirmBody: ({ title, refundText, feeText, remainingLost = false, noRemaining = false }) => {
                 if (noRemaining) {
                     return `Are you sure you want to cancel '${title}'? There are no remaining funds to return.`;
diff --git a/static/my_spots.js b/static/my_spots.js
index 04edf481a7d53553304e6a53fbc9adfc1cae8093..57b2841f8a61b12bf20cf53c8acfcd4df7ff1824 100644
--- a/static/my_spots.js
+++ b/static/my_spots.js
@@ -1117,17 +1117,33 @@ function closeCancelModal() {
     els.cancelBackdrop.hidden = true;
 }
 
-function setCancelBodyContent({ title, refundText, feeText, remainingLost, noRemaining }) {
+function setCancelBodyContent({
+    title,
+    refundText,
+    feeText,
+    remainingLost,
+    noRemaining,
+    manualReviewRequired,
+}) {
     if (!els.cancelBody) return;
 
     if (noRemaining || remainingLost) {
-        els.cancelBody.textContent = TEXT.cancelSpot.confirmBody({
+        const lines = [TEXT.cancelSpot.confirmBody({
             title,
             refundText,
             feeText,
             remainingLost,
             noRemaining,
-        });
+        })];
+        if (manualReviewRequired) {
+            lines.push(TEXT.cancelSpot.manualReviewNotice);
+        }
+        els.cancelBody.replaceChildren(...lines.map((line) => {
+            const span = document.createElement('span');
+            span.className = 'cancel-spot-body-line';
+            span.textContent = line;
+            return span;
+        }));
         return;
     }
 
@@ -1136,6 +1152,9 @@ function setCancelBodyContent({ title, refundText, feeText, remainingLost, noRem
         `Estimated refund: ${refundText}.`,
         `Cancellation fee: ${feeText}.`,
     ];
+    if (manualReviewRequired) {
+        lines.push(TEXT.cancelSpot.manualReviewNotice);
+    }
 
     els.cancelBody.replaceChildren(...lines.map((line) => {
         const span = document.createElement('span');
@@ -1156,6 +1175,7 @@ function openCancelModal(spot) {
     const feeText = nimFromLunaText(cancellation.fee_amount || cancellation.configured_fee || 0);
     const remainingLost = Boolean(cancellation.remaining_lost) || (remainingAmount > 0 && refundAmount <= 0);
     const noRemaining = remainingAmount <= 0;
+    const manualReviewRequired = Boolean(cancellation.manual_review_required);
 
     els.cancelTitle.textContent = TEXT.cancelSpot.title;
     setCancelBodyContent({
@@ -1164,6 +1184,7 @@ function openCancelModal(spot) {
         feeText,
         remainingLost,
         noRemaining,
+        manualReviewRequired,
     });
     els.cancelConfirm.textContent = TEXT.cancelSpot.confirm;
     els.cancelCancel.textContent = TEXT.cancelSpot.cancel;
diff --git a/tests/test_spot_creation_fees.py b/tests/test_spot_creation_fees.py
index 37662fc49e00f97dfc361bf8b23a85390b9ecc54..afdc50d7df6ab2c1968c2c9feaee46b1415d9c3a 100644
--- a/tests/test_spot_creation_fees.py
+++ b/tests/test_spot_creation_fees.py
@@ -278,6 +278,35 @@ class CreationFeeFundingTests(SpotCreationFeeFixture):
                         tx_hash="wrong-recipient",
                     )
 
+            with self.assertRaisesRegex(ValueError, "sender does not match"):
+                async with db_access.transaction(db, immediate=True):
+                    await db_access.create_spot_creation_fee_transaction(
+                        db,
+                        user_id=self.owner_id,
+                        spot_id=spot_id,
+                        amount=fee,
+                        from_address="NQ45 1KUT 73F7 ADV4 UCT8 TX64 2DE4 CHBP SJBF",
+                        to_address=str(spot[schema.SPOT_CREATION_FEE_ADDRESS]),
+                        tx_hash="wrong-sender",
+                    )
+
+            other_owner_id = await db_access.create_user(
+                db,
+                device_id_hash=f"other-owner-{time.time_ns()}",
+            )
+            await db.commit()
+            with self.assertRaisesRegex(ValueError, "user does not match"):
+                async with db_access.transaction(db, immediate=True):
+                    await db_access.create_spot_creation_fee_transaction(
+                        db,
+                        user_id=other_owner_id,
+                        spot_id=spot_id,
+                        amount=fee,
+                        from_address=str(spot[schema.SPOT_DEPOSIT_ADDRESS]),
+                        to_address=str(spot[schema.SPOT_CREATION_FEE_ADDRESS]),
+                        tx_hash="wrong-owner",
+                    )
+
     async def test_publish_waits_for_fee_confirmation(self):
         fee = const.LUNA_PER_NIM
         spot_id = await self.create_standard_spot(fee=fee)
@@ -418,7 +447,7 @@ class CreationFeeCancellationTests(SpotCreationFeeFixture):
         required = db_access.spot_required_deposit_amount(spot)
         await self.create_deposit(spot_id, required)
         await self.create_creation_fee_transaction(
-             spot_id,
+            spot_id,
             status=const.TRANS_STATUS_CONFIRMED,
         )
 
@@ -546,11 +575,11 @@ class CreationFeeCancellationTests(SpotCreationFeeFixture):
 
         self.assertEqual(final_spot[schema.SPOT_STATUS], const.SPOT_STATUS_CANCELLED)
 
-    async def test_funded_draft_cannot_be_deleted_but_failed_deposit_does_not_block_delete(self):
+    async def test_any_deposit_history_blocks_delete_and_failed_only_draft_is_archived(self):
         funded_id = await self.create_standard_spot(fee=0)
         await self.create_deposit(funded_id, const.LUNA_PER_NIM)
         async with schema.get_db() as db:
-            with self.assertRaisesRegex(ValueError, "funded drafts cannot be deleted"):
+            with self.assertRaisesRegex(ValueError, "deposit history cannot be deleted"):
                 await db_access.delete_draft_spot(db, spot_id=funded_id)
 
         failed_id = await self.create_standard_spot(fee=0, title="Failed Deposit")
@@ -560,9 +589,27 @@ class CreationFeeCancellationTests(SpotCreationFeeFixture):
             status=const.TRANS_STATUS_FAILE