name: Final polish patch (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain status
        run: echo "This superseded workflow is intentionally disabled."
