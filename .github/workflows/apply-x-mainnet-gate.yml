name: Apply X production-mainnet gate

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: apply-x-production-mainnet-gate
  cancel-in-progress: true

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: agent/automatic-x-posts
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - run: python scripts/apply_x_mainnet_gate.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Gate X posting to production MainAlbatross"
          git push origin HEAD:agent/automatic-x-posts
