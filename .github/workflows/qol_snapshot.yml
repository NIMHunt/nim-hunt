name: QoL Snapshot

on:
  push:
    branches:
      - agent/readme-prizedraw-localisation

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Create source archive
        run: |
          tar \
            --exclude=.git \
            --exclude=venv \
            --exclude='**/__pycache__' \
            --exclude='*.pyc' \
            --exclude=records.db \
            -czf /tmp/nimhunt-qol-source.tar.gz .

      - name: Upload source archive
        uses: actions/upload-artifact@v4
        with:
          name: nimhunt-qol-source
          path: /tmp/nimhunt-qol-source.tar.gz
          retention-days: 1
